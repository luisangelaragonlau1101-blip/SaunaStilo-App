// No employee credentials, records, alarms or private writes. Inspect transport, not session values.
const {chromium}=require('playwright'),fs=require('node:fs');
(async()=>{const report={requests:[],direct:[],errors:[]};const root='https://ollin-smart-vxs23c.v2.appdeploy.ai';const browser=await chromium.launch({executablePath:'/usr/bin/google-chrome',headless:true,args:['--no-sandbox']});try{for(const origin of [root,'https://sauna-stilo-app-web.vercel.app']){const r=await fetch(root+'/api/sauna',{method:'POST',headers:{'Content-Type':'application/json',Origin:origin},body:JSON.stringify({action:'status'}),signal:AbortSignal.timeout(12000)});report.direct.push({origin,status:r.status,type:r.headers.get('content-type'),cors:r.headers.get('access-control-allow-origin'),error:(await r.text()).slice(0,180)});}const page=await browser.newPage();page.on('pageerror',e=>report.errors.push(e.message));page.on('request',async r=>{if(r.method()==='POST'){const u=new URL(r.url());report.requests.push({method:r.method(),origin:u.origin,path:u.pathname,queryKeys:[...u.searchParams.keys()],headerNames:Object.keys(await r.allHeaders()),bodyKeys:(()=>{try{return Object.keys(r.postDataJSON()||{});}catch{return [];}})()});}});await page.goto(root+'/?workspace=sauna-stilo');await page.getByRole('textbox',{name:'Pregunta a Online Smart'}).fill('Saluda al equipo en una oración.');await page.getByRole('button',{name:'Enviar pregunta',exact:true}).click();await page.waitForTimeout(9000);report.sameOrigin=await page.evaluate(async()=>{const r=await fetch('/api/sauna',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'status'})});return{status:r.status,type:r.headers.get('content-type'),body:(await r.text()).slice(0,180)};});report.scriptUrls=await page.locator('script[src]').evaluateAll(xs=>xs.map(x=>{const u=new URL(x.src);return u.origin+u.pathname;}));report.appGlobalKeys=await page.evaluate(()=>Object.keys(window).filter(k=>/appdeploy|config/i.test(k)));}finally{fs.writeFileSync('transport-report.json',JSON.stringify(report,null,2));console.log(JSON.stringify(report,null,2));await browser.close();}})().catch(e=>{console.error(e.message);process.exitCode=1;});
