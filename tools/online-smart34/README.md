Private service source mirror for existing AppDeploy app ollin-smart-vxs23c, version 1788743552285. This is reviewed code, not a Firebase deployment. The AppDeploy backend uses SDK db, ai.generate, router/json/error and unpdf@1.8.1. Credentials are not embedded; CLIENT_KEY is the existing public Firebase web configuration, never a service account. Callers send their own short-lived session over HTTPS. Run isolated tests with node --test tools/online-smart34/service.test.mjs. The live entrypoint spreads saunaRoutes({db,ai,json,error,parsePdf}) into the existing router without replacing /api/chat or the public frontend. No employee records, manuals or certificates are seeded by these tests.
