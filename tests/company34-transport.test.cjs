const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs');
test('private training and manual calls use the app-scoped API host, not the static website',()=>{const s=fs.readFileSync('lib/services/company_learning_service.dart','utf8');assert.ok(s.includes('https://api-v2.appdeploy.ai/app/ollin-smart-vxs23c/api/sauna'));assert.ok(!s.includes('v2.appdeploy.ai/api/sauna'));assert.ok(s.includes("'X-Sauna-Token':token"));assert.ok(s.includes("if(token==null||token.isEmpty)"));assert.ok(s.includes('FirebaseAuth.instance.currentUser?.uid!=user.uid'));});
