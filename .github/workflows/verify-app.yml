name: Verify application
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: verify-${{ github.ref }}
  cancel-in-progress: true
jobs:
  team-workflows:
    uses: ./.github/workflows/team-release-review.yml
