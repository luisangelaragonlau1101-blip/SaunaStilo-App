name: Team final archive
on:
  push:
    branches: [fix/intro-online-smart]
    paths: [.github/workflows/team-final-archive.yml]
permissions:
  contents: read
jobs:
  archive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: git archive --format=tar HEAD > source.tar
      - uses: actions/upload-artifact@v4
        with:
          name: final-source
          path: source.tar
          retention-days: 1
